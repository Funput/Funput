// Entry point compiled by build.rs. Re-exports Windows Slint roots for Rust.

import { SettingsWindow } from "shell/settings.slint";
import { OnboardingWindow } from "onboarding/window.slint";
import { ControlCenterWindow } from "control_center/window.slint";
import { Compose } from "overlays/compose.slint";
import { AppEntry, ShortcutEntry, Theme } from "theme/mod.slint";

export {
    AppEntry,
    ShortcutEntry,
    OnboardingWindow,
    Compose,
    SettingsWindow,
    ControlCenterWindow,
    Theme,
}
