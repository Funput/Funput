package app.funput.funput.keyboard

/** Semantic haptic events shared by every Funput keyboard panel. */
enum class KeyboardHapticType {
    KEY_PRESS,
    CONTROL,
    DELETE,
    DELETE_REPEAT,
    SUBMIT,
}
