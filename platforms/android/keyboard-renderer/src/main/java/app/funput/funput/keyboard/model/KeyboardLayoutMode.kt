package app.funput.funput.keyboard.model

/** Canvas layouts that share the keyboard interaction and rendering pipeline. */
enum class KeyboardLayoutMode {
    LETTERS,
    SYMBOLS_PRIMARY,
    SYMBOLS_SECONDARY,
}
