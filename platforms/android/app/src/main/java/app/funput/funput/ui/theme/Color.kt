package app.funput.funput.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * The four brand colours, used for decoration that stays recognisably Funput no matter which
 * colour scheme is active: the settings icon tiles, the hero wash and the setup gradients.
 *
 * Scheme roles are not defined here — they live in [FunputLightColors] and [FunputDarkColors],
 * whose tonal palettes are generated from these same seeds.
 */
internal val BrandOrange = Color(0xFFFFA43F)
internal val BrandPink = Color(0xFFFF4F78)
internal val BrandPurple = Color(0xFF9F00EE)
internal val BrandBlue = Color(0xFF1F78FF)
