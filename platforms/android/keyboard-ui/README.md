# Keyboard UI

Hosts Funput's keyboard panels and switches between them without leaving the IME
window. The latency-sensitive QWERTY surface remains in `keyboard-renderer`; this
module owns higher-level UI such as the lazy-loaded AndroidX emoji picker.
